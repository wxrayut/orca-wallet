"use client";

import * as React from "react";

// import { PublicStateContext } from "~/contexts/system";

export function usePublic() {
    // const ctx = React.useContext(PublicStateContext);
    // if (!ctx) {
    //     throw new Error("usePublic must be used within a PublicStateProvider");
    // }
    // return ctx;
}
